# 2 周吃透这份设计稿 · 冲刺路径

生成日期：2026-09-17
适用前提（按你的回答校准）：**相关领域都接触过 → 跳过所有基础铺垫；1–2 周冲刺 → 只做「吃透 + 扛追问」；不动手写系统 → 不做工程实现，但要能在白板上画、在口头讲清取舍。**
唯一的输入文件：`智能预约Agent系统设计.md`（v2，689 行）+ `appointment-architecture.html` + `verify/mermaid/*.svg`

每天预算：2–3 小时。**能过自测就跳，不要为了"走完流程"重复读已经会的东西。**

---

## 策略：这份稿子该怎么学

**不要从头读到尾。** 689 行的读法有两种，差别很大：

- ❌ 顺序通读 → 读到 §8 时已经忘了 §4 的模块权责；面试时能想起"好像有这么个设计"，但讲不出因果。
- ✅ **按"一条主链路 + 四个必被打的点"读** → 每一节都在回答一个具体追问，读完就能复述。

你的三个自测维度（每节都用这三个尺子量）：

1. **能默认复述**：合上文档，90 秒讲清这一节在解决什么问题。
2. **能说出代价**：这个选择放弃了什么？（例：选模块化单体 → 放弃了独立扩缩容；选重建式恢复 → 放弃了 SDK 的跨中断 token 计数连续性。）
3. **能说出改判条件**：什么情况下我会换另一条路？（这是面试官最想听的，也是最能区分"背稿"和"真懂"的一句。）

---

## 第 0 步 · 先堵 5 个洞（半天，别跳过）

**为什么先做这个**：现在稿子里有 5 处未闭合的地方（上一轮评审 R1–R5）。**先把口头答案准备好再背稿**，否则你背熟的 §5.2 和 §8.3 在被追问时会当场漏气。这一步不用改文档，只要能答。

| 洞 | 你会被怎么问 | 必备口头答案（三句式） |
|---|---|---|
| **R1 两套「等待」机制** | "用户 10 分钟后回了个'好'，你是续跑挂起的 reply，还是重建上下文？" | ① 补一句设计决定：**业务账本为准，SDK 挂起态视为可丢弃的瞬时态**，恢复走"先读结构化状态和订单，再构建模型上下文"。② 代价：放弃 SDK 跨中断的 token 计数连续性（改为在 task 账本累计）。③ 若选续跑：必须持久化完整 `AgentState`，且要知道续跑时新传入的 `structured_schema` 会被忽略、沿用挂起那份，这会影响 release 固定策略。 |
| **R2 `AskUser` 的伪确认** | "SDK 的 `AskUser` 返回结构化的 answers，程序可以直接判断用户同意了，这不就够了？" | ① `AskUser` 的答复是**用户输入事件**，只作槽位补丁/意图信号。② 业务确认必须绑定 `proposal_id/version/内容哈希` 并由领域服务在提交事务内重校验——因为工具级许可 ≠ 方案级授权。③ 另外 `AskUser` 是 external tool，会让 reply 挂起，所以用它做追问就把 R1 的问题一起背上了。 |
| **R3 outbox 缺供应商字段** | "你说要跟供应商对账，对账的依据存哪张表？" | 承认 §6.1 的 `outbox/job` 目前只有投递侧字段（投递状态/重试/下次执行时间），**供应商侧字段（provider_message_id / receipt_status / reconciled_at）要补**；并说明这是"设计待补"而非"已经实现"。 |
| **R4 门店/坐席侧无契约** | "§3.1 说门店员工也走同一领域服务，那前台怎么把订单置成已完成？" | 承认 §9 目前只列了 9 个用户侧工具，**门店/坐席侧命令（闭店、请假、改派、核销、置 NO_SHOW）需要补一张小表**，并同时定义坐席的 PII 可见范围与审计要求。 |
| **R5 延迟预算口径** | "候选流程 P95 ≤8 秒，你的预算是模型 5 秒 + 查询 1 秒 + 开销 2 秒，加起来正好 8 秒？" | 澄清 8 秒是**单次请求的硬预算上限**（超出按 §5.3 分类返回），"P95 ≤8 秒"是**待实测目标**，两者不是同一个东西；若坚持把预算当期望值，则等于零余量。 |

**第 0 步通过标准**：5 个洞都能在 30 秒内用"结论 + 代价 + 改判条件"答完，且**不否认问题存在**。面试里承认"这一处我设计里没闭合、我会这样补"比嘴硬安全得多。

---

## 阶段 1 · 建立骨架（第 1 天）

**读**：§1 决策摘要 → §3.1 不变量 → §4 模块表 → §4.1 复用边界 → §16 两分钟讲稿

**为什么这个顺序**：先看"我做了什么决定"（§1），再看"我必须保证什么"（§3.1），然后才看"我用什么结构保证"（§4）。顺序反了就会把架构当成品味，而不是当作不变量推导出来的结果。

**今天必须默写出来的两样东西**：

1. **§3.1 的 8 条不变量**（这是全篇的宪法，后面每一节都是它的实现）。默写时按"资源 / 授权 / 幂等 / 事实来源 / 入口唯一 / 承诺边界 / 失败语义 / 可追溯"分组记。
2. **§16 两分钟讲稿**（背到能自然说出，不是逐字背）。讲稿里已经包含了最关键的四个词：*任务状态持久化、确认绑定方案、区间排他约束、业务幂等键*。

**自测（全过的就跳过第 2 天）**：
- 不看文档，说出 8 条不变量中的 6 条以上。
- 说出"用户、门店员工、后台任务都必须经过同一预约领域服务"这条不变量存在的理由（答：否则聊天入口守规矩而人工后台绕过约束，正确性就没有单一入口）。
- 默画 §4 的主链路：客户端 → 接入 → 编排 → 工具边界 → 领域服务 → PostgreSQL，并说出编排器与 Agent 谁调谁。

---

## 阶段 2 · 核心难点：事务与并发（第 2–3 天，最重要）

**读**：§6.1 实体表 → §6.2 三类状态 → §8 全节（8.1/8.2/8.3/8.4）→ §18.3 图

**这是整份稿子的重心**，也是面试官最容易连续追问的地方。为什么：因为这里是你相对两个开源项目的**真正增量**——原型连订单聚合都没有（`save_appointment` 只用毫秒时间戳当 ID，`models.py` 没有 Appointment 表），所以你做的不是优化，是结构性重建。

**必答题清单（每题都要能讲 90 秒，带取舍）**：

| 问题 | 答案骨架 |
|---|---|
| 两个用户同时约同一个人？ | 查询都可看到空闲 → 占位事务由数据库区间排他约束裁决 → 失败方拿 `SLOT_CONFLICT` 重新规划。**关键**：候选查询是时间点快照，不构成承诺。 |
| 为什么 Redis 锁不够？ | 锁租约失效、实例暂停、绕过入口都会发生；**数据库约束覆盖所有正规写入**。Redis 只削峰，不是裁决者。 |
| 过期的 HELD 会不会一直占资源？ | 不依赖 Worker 存活：**查询端忽略逻辑过期，写入端在同一事务内先回收再占位**，加锁后用实际当前时间判断（不能用事务起始时间）。 |
| 为什么不能把 `now()` 放进索引谓词？ | 想让索引"随时间自动变化"是错的；过期靠稳定状态 + 显式回收，不靠时间函数。 |
| 同一个确认提交 100 次？ | 幂等键由**应用为一次确认分配**（tenant+task+proposal_version+action），不是每次生成新 UUID；operation 唯一约束 + 同事务结果 ⇒ 原结果重放。 |
| 提交后响应丢了？ | 用原键查 operation（读权威主库，不能读只读副本判定"未下单"），返回同一订单。**不换键重试**。 |
| 用户确认了但技师请假？ | 提交事务内重校验**依赖事实**（资源/班次/营业/报价），失效则 `STALE_PROPOSAL`。**注意**：价格按"有效期内锁价"，新价目表上线不应撤销仍有效的旧报价——这一条你要讲得比别人细。 |
| 确认凭据能被模型伪造吗？ | 不能。凭据绑定 tenant/actor/task/proposal 版本/内容哈希/nonce；`LLM 输出的 confirmation=true 不具有授权效力`。 |

**必须能画出来的图**：§18.3 的两请求争抢（打开 `verify/mermaid/18-3-资源争抢时序.svg` 看渲染结果，然后自己白板复现）。画的时候要能指出三处：① 查询快照不等于承诺；② 锁等待；③ 排他约束作为最终兜底。

**通过标准**：以上 8 题任抽 3 题，每题 90 秒内答完且**主动说出至少一个代价或反例**（例："所以我不宣称端到端 exactly-once，只宣称至少一次送达 + 业务效果幂等"）。

---

## 阶段 3 · Agent 与 SDK 边界（第 4 天，最容易被质疑的一节）

**读**：§2 → §4.1 → §4.2 → §5 全节（含入口分类段、5.1/5.2/5.3）→ §9

**为什么这节危险**：面试官看到"用 AgentScope"会立刻问"为什么不用它自带的 Agent Service / 为什么不用 LangGraph"。稿子已经答得不错，但答得对不对取决于你**能不能说出被否掉的方案的优点**。

**必答题**：

| 问题 | 答案骨架 |
|---|---|
| SDK 有 Agent Service，你为什么自己写入口？ | ① 它是 `app/` 平台能力不是纯 SDK API：会话、消息总线、渠道、workspace 工具组装都在里面。② 首版只做 Web、工具集固定、业务确认占核心 → 选嵌入模式，避免默认工作区工具和第二套会话管理。③ **代价要说**：窄接口、SDK 快照持久化适配、事件投影都要自己维护。④ 改判条件：多渠道与工作区成为核心时整体评估。 |
| 为什么不直接上 LangGraph / Temporal？ | 两者都是有效选项，不是"不能持久化"。**LangGraph**：图状态/检查点/HITL 需求强时评估；**Temporal**：跨系统长期工作流时评估，且要注意 workflow 确定性、副作用必须放 Activity、重试要传同一个业务幂等键。当前有限状态机先留在业务层，通过 `WorkflowPort` 可替换。**结论句**：真正不变的是领域约束与幂等效果，不是框架。 |
| 为什么只有两个在线 Agent？ | 接待（理解+提议）与咨询（只读检索、独立知识上下文+只读权限）职责与权限不同才值得拆；**提交/可用性/金额/确认校验用普通代码**——把它们叫 Agent 不增加业务价值。 |
| 怎么证明两个 Agent 比一个好？ | 同工具、同数据集、同预算做消融，比成功率/延迟/成本/失败归因；**没有可重复收益就合并**。 |
| SDK 的能力具体落在哪？ | 能背出 4–5 个映射：`on_check_permission` 接工具权限、`on_acting` 记工具尝试与结果、`on_model_call`+`ModelCallEndEvent` 关 model_call 记账、`ReplyBudgetControlMiddleware` 管 reply 级 token 预算（**不是** task 总预算、**不是**费用硬上限）、`AgentState` 是执行快照（业务订单和授权始终在领域库）。 |
| 为什么要自己持久化，SDK 不是有状态管理吗？ | 模型执行上下文 ≠ 业务状态。**框架恢复不能保证外部调用只产生一次效果**，也不能替代用户确认和订单状态。 |

**通过标准**：能画出 §5.2 那张表的前 5 行（SDK 能力 → 落点 → 限制），并且能主动说出**每一条限制**（比如"token 预算耗尽仍可能发生最后一次收尾调用""ASK ≠ 业务授权"）。

---

## 阶段 4 · 数据、检索、记忆（第 5 天）

**读**：§6.3 → §6.4 → §7 → §10（10.1/10.2/10.3）

**这节的重点是"边界划在哪"**，不是技术细节：

- **哪些进 RAG，哪些必须走结构化 API**：服务介绍/FAQ/注意事项 → RAG；**当前价格、优惠资格、排班、订单状态 → 结构化 API**。理由一句话："向量相似度不提供事务语义"。
- **规则与文案的单一真相源**：可执行规则（取消窗口、费用）以版本化结构配置为准，关键数值由配置渲染，运营不手写第二份；**但要说清它只消除"重复录入数值"的漂移，模板语义与配置错误仍需测试与审批**——不要说成"结构上不可能不一致"。
- **中文检索**：不能拿 PostgreSQL 默认英文全文检索当中文 BM25；按"PG 分词扩展 / 应用层分词倒排 / 独立搜索引擎"三选一，判据是部署权限、租户过滤、词典维护、可测效果；**并且要有 FAQ/结构化检索基线**。
- **记忆**：首版不用通用长期记忆后端，用 `preference` 表保证来源/纠正/时效可审计。当前明确指令 > 历史偏好；显式偏好 > 推断偏好；用户纠正后**不得从旧历史反向覆盖回来**。

**必答**：
1. "『明天下午 3 点』在用户和门店不同时区时按谁解析？" → 默认门店时区且界面与确认卡都显示该口径；用户明确"按我这儿的时间"才显式转换并展示两地时间；DST 不存在的时间要**拒绝并建议替代**，重复出现的时间要用户选 offset，**不静默猜测**。
2. "价格表能不能切块进 RAG？" → 不能切成失去表头或适用条件的碎片。
3. "RAG 检索到错误政策怎么办？" → 先校验租户/门店/版本/生效区间；有冲突或缺证据就拒绝自动判定；**可执行规则由业务配置决定，不由文档决定**。

---

## 阶段 5 · 恢复、安全、发布、评测（第 6–7 天）

**读**：§11 + §11.1 → §5.3 → §12（12.1–12.4）→ §13（13.1–13.3）→ §14

**这三天里含整份稿子最出彩的一节**：§11 的「故障 → 系统行为 → **不该做的事**」三列表。把它当真背，面试时能显功力——因为它证明你在区分"能跑"和"正确"。

**必须背下来的**：§11 那张表里的"不该做的事"列（把依赖失败说成没有号、偷换技师完成下单、生成新键重新创建、依赖内存 finally 释放、从聊天摘要猜测用户是否确认、回滚已确认订单、把网络断开当作取消）。

**§11.1 的恢复 trace 要能按编号复述**：提交成功 → 发成功事件前进程退出 → 以相同幂等键查主库找到原订单 → 返回原订单 → 任务条件更新（若有更高版本则事件归并，**不覆盖用户后续改约意图**）→ Worker 重复领取靠投递键与供应商能力控制重发。

**必答**：
1. "通知发送失败，订单要回滚吗？" → 不回滚；订单已成功是事实；发送独立重试或人工补发。**并且要主动说残余风险**：供应商若不提供幂等键或查单能力，"已发送但回执丢失"时无法保证绝不重复，靠对账 + 产品兜底。
2. "模型全挂了怎么办？" → 切结构化表单 + 人工入口，**继续用同一领域服务、确认和事务约束**；FAQ 返回已发布的确定答案，关闭自由生成。
3. "超时了怎么答？" → 按事实分类：没写入→"本次未完成"；已回滚→失败；已提交→成功；执行中/结果不明→`PROCESSING`/`UNKNOWN_OUTCOME` + operation_id。**停止生成不等于撤销已提交副作用。**
4. "怎么上线一个提示词改动、怎么回滚？" → `release_manifest` 固定模型/提示词/工具 Schema/知识/规则版本；新任务分配 release_id 且**沿用不变**；先契约测试 + 离线回归 → 只读影子（**影子不调业务写工具**）→ 小流量灰度；回滚改新任务的指针，**不重放历史写入**；但紧急业务事实（停业、资源禁用、授权撤销）**始终用当前有效值校验，不因任务版本固定而忽略**。
5. "CI 门禁怎么设？" → 分两层：**确定性不变量（跨租户、未确认写入、重复效果、资源重叠）零违规为硬门禁**；LLM 语义与成本用固定留出集 + 多次运行 + 基线区间做软门禁，明显退化阻止灰度。
6. "400 个场景的 oracle 长什么样？" → 能背 `E-DUPLICATE-CONFIRM` 那个例子：同确认重放 100 次后 appointment 按 operation 只有一笔、有效 allocation 数等于所需资源数、operation 只有一个最终结果、所有响应引用同一订单、订单方案哈希与授权哈希一致；**检查关键动作偏序，不要求模型措辞唯一**。

---

## 阶段 6 · 追问对抗（第 8–9 天，价值最高）

**读**：§15 全部 + 本文件第 0 步的 5 个洞 + §17 + §18

**方法（重要）**：不要"看不做的复习"。用下面三种方式之一，逼自己**出声讲**：

1. **自问自答录音**：从 §15 + 本文件各处挑 15 题，每题对着手机录 60–90 秒，回听找三处问题：有没有结论句、有没有代价句、有没有改判条件句。
2. **找人连环追问**：把 §3.1 的不变量给对方，让对方随便挑一条问"你怎么保证"，答完继续追"那如果……"。
3. **反向讲解**：假装给对方讲这份设计，**故意讲你否掉的方案**（完整 Agent Service、纯表单、LangGraph、把一切塞进向量库、Redis 锁），看自己能不能说出对方方案的合理之处。

**这一步的验收标准**：任一问题都能用**三句式**回答——`结论 → 我否掉了什么/代价 → 什么条件下我会改`。这是你"讲清自己的取舍"这个目标的落地形式。

---

## 阶段 7 · 全真演练（第 10 天）

**流程（模拟真实面试 45 分钟）**：

1. **5 分钟**：不看稿讲 §16 两分钟版，扩到 5 分钟（加"核心难点"和"我的增量"）。
2. **10 分钟白板**：徒手画三张图——主链路（§18.1）、任务状态机（§18.2）、资源争抢（§18.3）。画完对照 `verify/mermaid/*.svg` 找漏掉的状态/边。
3. **20 分钟追问**：从 §15 + 阶段 3 表格 + 第 0 步 5 个洞里抽题。
4. **10 分钟收尾**：诚实说边界——"这是设计，没有实现、没有压测、没有跨框架基准；我能展示的是核验方法与不变量测试设计"。

**最后检查表（勾完就算准备好了）**：

- [ ] §3.1 八条不变量能默写
- [ ] 三张白板图能徒手画，且能指出每张图的"兜底机制"
- [ ] §8 的 8 个必答题能答
- [ ] 阶段 3 的 6 个 Agent 边界问题能答，且能说出被否方案的优点
- [ ] §11 的"不该做的事"能背
- [ ] 5 个洞（R1–R5）都能用"结论+代价+改判条件"答完，且不否认问题
- [ ] 能说清自己的贡献边界：**哪些是开源项目给的、哪些是我重建的、代价是什么**
- [ ] 全程不出现"上线实现了/提升了百分之多少"（因为确实没有）

---

## 附 A · 稿子之外必知（会被问，但稿子只假设你懂）

这些不需要系统学，只要在遇到时能用一句话解释清楚：

| 话题 | 你需要能说出的一句话 |
|---|---|
| PostgreSQL 区间排他约束 | 用 `EXCLUDE USING gist` 表达"同租户同资源的有效占用区间不得重叠"，标量等值配合 `btree_gist`，时间用半开区间 `[start, end)` |
| 为什么半开区间 | 相邻时段 `[9,10)` 与 `[10,11)` 不算重叠，避免边界把资源多占一分钟 |
| 事务隔离 vs 约束 | 隔离级别解决并发读写的可见性，**不变量要靠约束**；两者互补 |
| `now()` vs 实际时间 | `now()` 是事务起始时间，长时间等锁后可能已过时，有效期裁决要用实际时间点 |
| Outbox 模式 | 把"数据库写入"和"要发的事件"放同一事务，消费者至少一次处理、自身幂等；**不等于**事件溯源 |
| 幂等键 | 由应用为一次业务动作分配并复用，配唯一约束 + 请求哈希；同键同参返原结果，同键异参报冲突 |
| Little 定律 | `L = λW`：并发位置 ≈ 到达率 × 处理时长。用来估容量，不替代压测 |
| SSE 恢复 | 断线以最后确认的事件序号重连；**连接断开不代表业务取消** |
| HNSW vs 精确检索 | 近似检索快但叠加 ACL 过滤后可能召回不足 K 条，必须测过滤后的召回率 |
| OpenTelemetry | 用 span 串 trace；**trace 不自动等于计费持久化**，成本要单独记账 |

---

## 附 B · 背诵清单（只有这些值得逐字记）

其余都可以用自己的话讲，但这几处**措辞固定**更安全：

1. §3.1 的 8 条不变量（几乎会被逐条挑）
2. `模型理解需求、选择允许的查询、提出方案；业务服务验证权限与约束；数据库裁决资源占用；用户确认绑定具体方案`（§1 的四句核心原则）
3. `LLM 输出的 confirmation=true 不具有授权效力`（§8.3）
4. §11 表里"不该做的事"那一列
5. §13「有限样本的零违规不能证明真实世界绝对零风险」
6. §16 两分钟讲稿的最后一句：`目前这是系统设计，实际性能和收益需要实现后测量`
7. 五个术语的定义：`task / proposal / hold / resource_allocation / operation / confirmation`（§文首术语表，会被问"proposal 和 hold 什么关系"）

---

## 附 C · 只有 5–7 天时怎么压缩

| 保留 | 压缩/合并 |
|---|---|
| 第 0 步（堵洞）——**不能砍** | 阶段 4（数据/检索/记忆）压到 2 小时，只背"边界划在哪"三句 |
| 阶段 1 骨架 | 阶段 5 的 §13 评测只背"CI 两层门禁"和 1 个 oracle 例子 |
| 阶段 2 事务并发——**不能砍** | 阶段 6 的追问对抗从 2 天压到 1 天，但**必须出声讲** |
| 阶段 3 Agent 边界 | 白板只画 §18.2 状态机 + §18.3 争抢（主链路口述即可） |

**如果只能保 2 天**：第 0 步（半天）+ 阶段 2（半天）+ 阶段 3 必答题（半天）+ 白板三图与讲稿（半天）。这四项覆盖了面试里 80% 的追问密度。
