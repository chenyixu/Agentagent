# 智能预约 Agent 系统设计 · 评审与改进清单

评审日期：2026-09-17
评审对象：
- `Agentagent/智能预约Agent系统设计.md`（38,713 bytes）
- `Agentagent/appointment-architecture.html` / `appointment-architecture.json`（9 组件 8 连线，showcase 校验 9/9）
- `Agentagent/architecture-verification.json`（记录：未做视觉复核，未采集浏览器证据）

评审方法：通读设计稿；对两个来源项目读取本地 clone 源码核对（非仅 README）。
核对的 commit：
- `agentscope` → `033a3613401a3e6cbd39608321579481f6bd0953`（2026-09-09，`feat(tool): add AskUser`）
- `smart-appointment-ai-agent` → `2bd2dba67e46bf65d614ca7917d0005a45c6a25e`（2026-05-06）

---

## 0. 总体判断

**稿子的真实优势（要保住，别在改稿时丢掉）**

1. §3.1 用"不变量"定义系统，而不是用功能列表。这是架构稿的正确写法。
2. §8.1–8.3 的并发/幂等/确认三节，是目前最有价值的部分：区间排他约束 + GiST、半开区间、`now()` 不进索引谓词、幂等键由应用分配而非每次新 UUID、确认凭据绑定内容哈希、`LLM 输出的 confirmation=true 不具有授权效力`——这些都是"踩过坑才写得出来"的判断。
3. §11 的"故障 → 系统行为 → **不该做的事**"三列表，是整篇最出彩的一节。把"把依赖失败说成没有号""偷换技师完成下单"显式列为禁止行为，说明作者在区分"能跑"和"正确"。
4. §15 的问答已经在自我反驳（"没有收益就合并""不声称它天然优于所有框架"），这是加分姿态。

**主要风险：不是缺内容，是缺"交代"**

评审下来，真正会导致面试翻车的不是知识点，而是四类：
- **比较没做**：AgentScope 的 Agent Service 已经提供了和你的"接入层 + 会话真相源"高度重叠的能力，稿子只用半句话带过（§1）。面试官会认为你没读透 SDK，或认为你在重复造轮子。
- **图为文之误**：架构图有 6 处与正文/常识不一致（见 §1.3），架构图是面试官第一个看的东西。
- **表与表打架**：工具表里 `transfer_to_human` 返回 `case_id`，但 §6.1 实体表里没有 case 实体；§12.2/§13 要记 token/成本，但没有 `model_call` 记账实体（见 §1.4）。
- **举证的硬度不够**：§2 说"由这几处源码可推断"，但没给行号；而本地两个 clone 的 commit 是确定的，把 commit + 文件 + 行号钉进去是**零成本高收益**（见 §2.1，已替你写好可抄段落）。

**修完 §1 的 8 条，这份稿子从"很完整的设计"变成"经得起追问的设计"。**

---

## 1. P0 · 会被直接打穿的问题

### 1.1 【最高风险】与 AgentScope Agent Service 的边界没有交代

§1 原文只有一句：

> AgentScope 的 Agent Service 可作为后续渠道与工作区管理的参考，首版不同时维护两套入口和会话真相源。

但本地 clone 显示 `agentscope/app/` 已经是一个完整的多租户 Agent 服务平台，包含：

| AgentScope `app/` 已提供 | 你的设计里对应什么 |
|---|---|
| `_service/_session.py`、`_session_projection.py`（会话与投影） | §6.2 会话状态 |
| `message_bus/`（`_in_memory_message_bus.py` + `_redis_message_bus.py`） | §12.3 多副本协调 |
| `channel/`（`_feishu` / `_dingtalk` / `_discord` / `_gateway` / `_dispatcher` / `_routing` / `_stream` / `worker`） | §1 "渠道客户端"、P3 多渠道 |
| `access/_policy.py`（访问策略） | §12.1 权限 |
| `_jwt.py` / `_jwks.py` / `_auth.py` / `deps.py` | §5.1 第 1 步"从凭据解析 tenant_id/actor_id" |
| `workspace_manager/`（docker / k8s / e2b / daytona / opensandbox / bubblewrap / applecontainer / local + prewarm） | §12.3 部署与隔离 |
| `_index_worker.py` / `_index_task_consumer.py` / `_index_sweeper.py` | §11 Worker |
| `rag/`（`_knowledge.py` / `_document.py` / `_parser` / `_chunker` / `_vdb`） | §10.1 知识与检索 |
| `_credential_binding.py` / `_download_token.py` | §12.1 凭证服务端保留 |

**面试官一定会问**："SDK 已经有 session / message bus / channel / access policy / 索引 worker 了，你为什么还要自己写接入层和会话真相源？"

**现在稿子没有答案，只有一句结论。** 必须补一节"复用 vs 自建"的边界表。正确的、能自圆其说的划法（建议直接采用）：

| 能力 | 归属 | 理由（面试时要能一句说清） |
|---|---|---|
| 渠道接入（飞书/钉钉/Discord 等） | **复用 SDK** | 渠道是通信协议适配，不含业务语义，自研纯浪费 |
| 执行沙箱 / workspace 隔离 | **复用 SDK** | §9 首版不向 Agent 提供 Shell，workspace 更深的能力留给 P3 |
| JWT / JWKS / 凭证绑定 | **复用 SDK** | 认证是标准件 |
| 消息总线（多副本广播） | **复用 SDK，且默认 in-memory** | 确定性业务不需要跨副本广播，Redis 版是 P1 选项 |
| **任务账本（task/version/lease/fencing）** | **自建** | 业务级状态机与版本裁决，SDK 的 `AgentState` 是执行快照，语义不同 |
| **预约领域服务（可用性/占位/确认/改约）** | **自建** | 需要区间排他约束与事务，SDK 不提供领域不变量 |
| **确认凭据协议** | **自建** | 授权必须有业务语义（绑定 proposal 版本与内容哈希） |
| **Outbox / 通知投递** | **自建** | 与订单同事务写入，属于业务事务边界 |

这样写，从"半句带过"变成"我评估过、边界清楚、并说明了哪一半我不自研"——同样的决策，说服力完全不同。

### 1.2 SDK 的能力被"提起"但没被"用起来"——缺一张 SDK 映射表

§2 表格里写了"借鉴 AgentScope：Agent 执行、结构化输出、Toolkit、模型适配、事件、中间件、权限与人工介入"，但正文 §4–§12 完全没有把这些名字落到具体设计点上。面试官会追问"中间件用来干什么？""权限与人工介入具体怎么接？"

本地 clone 已核验的具体能力（**这些名字可以直接写进稿子，比泛泛而谈强得多**）：

| AgentScope 实际能力（已核验） | 你设计里的落点 |
|---|---|
| `MiddlewareBase` 的 **7 个 hook**：`on_reply` / `on_reasoning` / `on_check_permission` / `on_acting` / `on_model_call` / `on_compress_context` / `on_system_prompt` | §5.1 的执行协议：`on_check_permission` → 工具授权；`on_acting` → 工具结果入账本；`on_compress_context` → 上下文压缩策略 |
| `ReplyBudgetControlMiddleware`：**加权 token 预算**（`input_token_weight`/`output_token_weight`），状态存 `middle_context`，**明确支持跨 HITL 中断恢复** | §5.1 "最多 6 次工具调用 / 20 秒总预算"应改成：**步数预算（应用层）+ token 预算（SDK 中间件）双轨**，并说明 HITL 恢复时预算不清零 |
| `permission/_types.py`：`PermissionMode{DEFAULT, ACCEPT_EDITS, EXPLORE, BYPASS, DONT_ASK}`、`PermissionBehavior{ALLOW, DENY, ASK, PASSTHROUGH}`；`permission/_engine.py`（848 行） | §12.1 "默认拒绝"应该直接映射为 **`PermissionMode.EXPLORE` 用于咨询 Agent（只读）、`DEFAULT` 用于接待 Agent**；`ASK` → 对应 §8.3 的人工确认；**`DONT_ASK` 对应无人值守的 Worker**（这是很漂亮的一一对应，面试时是加分点） |
| `state/_state.py` → `AgentState`：`session_id` / `summary` / `context` / `reply_context(cur_iter, structured_schema, structured_output)` / `tool_context` / `tasks_context` / `permission_context` / `middle_context` | §6.3 "摘要不负责保存确认状态"应该说清分工：**`context`+`summary` 是 token 层，`tool_context`/`tasks_context` 是执行层，业务事实在 PostgreSQL**。这也正好回答 §15 的"为什么 SDK 有状态管理还要自己持久化" |
| `tool/_builtin/`：`_bash.py` / `_write.py` / `_edit.py` / `_read.py` / `_glob.py` / `_grep.py` / `_ask_user.py` / `_skill.py` | **见 §1.8，这是个必须显式处理的坑** |
| `tool/_task/`：`_create_task` / `_get_task` / `_list_task` / `_update_task` | §6.1 的 `task` 实体与 SDK 的任务工具是两层，要说明不混用 |
| `middleware/_longterm_memory/`：`_agentic_memory` / `_mem0` / `_reme` 三种后端 | §10.2 长期记忆应说明：**首版不用通用长期记忆后端，只用结构化 `preference` 表**，这是有理由的不采用（可审计 vs 不可审计） |
| `middleware/_tracing/`：OpenTelemetry（`opentelemetry-api>=1.39.0` 等已是硬依赖） | §12.2 观测可直接复用，不需要自研 trace 注入 |
| `event/_event.py`：`ReplyStartEvent` / `ReplyEndEvent` / `ModelCallEndEvent` 等 | §9 的 SSE 事件模型应对齐 SDK 事件，而不是自造一套 |
| `agent/__init__.py`：`Agent` / `A2AAgent` / `RealtimeAgent` / `TurnAggregator` / `TurnMetrics` / `ContextConfig` / `InjectionConfig` / `ModelConfig` / `ReActConfig` | §1 说"Agent Service 作为后续参考"时要知道 **A2A 协议与实时语音（`realtime/` + `tts/`）已经在 SDK 里**，P3 的"语音"成本被高估了 |
| `formatter/` + `pipeline/`（多 Agent 消息格式与编排） | §5 "两者通过结构化结果协作，不广播完整聊天历史"应对应到 `formatter`，说明用 SDK 能力而不是自己拼消息 |

**结论**：把上表压缩成稿子里的一节（约 1 页），这份稿子对 SDK 的理解深度会立刻从"读过 README"变成"读过源码"。

### 1.3 架构图与正文的 6 处不一致（架构图是面试官看的第一个东西）

基于 `appointment-architecture.json` 逐条核对：

1. **缺"到 Agent 层"的连接。** 图里 `agents`（AgentScope 执行层）只连了 `knowledge`（只读调用），与 `domain`（预约领域服务）**没有任何连线**。但 §9 明确接待 Agent 会调 `create_hold` / `confirm_appointment`。看图的人会以为 Agent 不碰业务工具。→ 补 `agents → orchestrator`（标注"工具请求经工具边界"）或 `agents → domain`（标注"仅经工具边界"）。
2. **缺事件回流方向。** `client → entry` 标了 "HTTPS / SSE"，但 SSE 是**服务端推给客户端**的。图上没有 `entry → client` 的反向事件通道。→ 补一条反向边，标注"任务事件推送"。
3. **`jobs` 只有一条入边 `ledger → jobs`（虚线，持久事件）。** 但 §11 说 Worker 还要做"到期释放占位"和"偏好候选"——这两件事是**基于时间扫描任务表**触发的，不是 Outbox 驱动的。→ 补 `state → jobs`（到期扫描）。
4. **图里没有"通知渠道"。** §11 整节在讲提醒与发送失败，§9 的 SSE 也是通道。图里 9 个组件没有任何一个负责发短信/推送。→ 加 `notice`（通知网关），`ledger → notice`（Outbox 消费）。
5. **图里没有"人工坐席"。** §11 有整段"人工接管"，工具表有 `transfer_to_human`，但图上 `client` 只写了"用户与门店前台"。→ 把 `client` 拆开或加 `console`（坐席工作台）。
6. **"AgentScope 执行层"画成了独立组件，正文说的是同进程调用。** §4 写"编排器按需调用 AgentScope Agent"，即库调用；图上是并列盒子，容易被理解成独立服务。→ 组件 sublabel 改为「AgentScope SDK（同进程库调用）」，或在图注说明。

另外 `architecture-verification.json` 自己记了 `"visual_review": "not completed"`，且 `browser_evidence: not collected`。面试材料里带着这条记录是好事（诚实），但如果要把这张图当亮点展示，**至少把视觉复核补上**，否则交付链路上是断的。

### 1.4 表与表自相矛盾：实体表缺 5 个实体

§6.1 是最容易被逐行追问的表。目前缺：

| 缺的实体 | 为什么必须有 |
|---|---|
| `user` / `customer` | 全篇都在说"用户偏好""用户历史订单"，但用户实体不存在。§6.1 只有 `conversation`，隐含未定义 |
| `store_business_hours` / `holiday` | §6.1 把营业规则塞在 `store` 的"营业规则版本"里；§7 第 4 步要"查询当前班次"、§3.1 有"营业窗口"不变量，节假日/临时闭店没有落点 |
| `handoff_case` | **§9 工具表里 `transfer_to_human` 必须返回 `case_id`，但 §11 只讲了接管机制，没有 case 实体。面试官翻到这里必问"case_id 存哪张表"** |
| `model_call`（记账） | §12.2 要求记录 token/延迟/错误类型，§13 要求报告"每轮和每个成功任务的模型、embedding、重排、重试成本"，§3.2 要求"根据真实输入输出 token 校核配额"——**没有表就全是空话** |
| `eval_case` / `annotation` | §13 全部建立在"约 400 个固定多轮场景"和"测试 oracle 核对最终数据库"上，但没有评测数据模型与 oracle 的编码方式 |

建议把 §6.1 从 13 行扩到 18 行。**这一节补完，§13 的评测才不是悬空的。**

### 1.5 提交前缺"重校验外部事实"这一步（TOCTOU 的另一半）

§8.2 说提交事务要"原子校验当前任务 epoch、方案版本和确认记录"——这防的是**用户侧**变更。
但没防**外部事实在"确认 → 提交"之间变化**：

- 用户确认了"周五 19:00 小王"，占位是 HELD；
- 在此期间技师请假 / 门店临时闭店 / 服务价格调价（`quote_version` 变了）；
- 用户点确认 → 直接按旧 proposal 提交 → **约上了一个已经不该成立的方案**。

正确的做法是：**提交事务内必须重新读取并校验 proposal 所依赖的事实**（资源是否仍有效可用、`quote_version` 是否仍是当前版本、门店营业规则版本是否未变），任一失效 → 返回 `STALE_PROPOSAL`，让用户重新确认。

稿子里有 `STALE_PROPOSAL` 这个错误码和"关键条款变化时旧方案及旧确认失效"，但**没有把"提交时重校验"写进流程**。这是一个非常典型的面试追问点（"用户确认了但没约上，是谁的锅？"）。

→ 在 §8.2 提交事务的校验清单里加上：`任务 epoch + 方案版本 + 确认记录 + 依赖事实（资源有效 / quote_version / 营业规则版本）`。

### 1.6 过期占位与排他约束的交互没讲透（Worker 挂了会怎样）

§8.1 说得很对："过期不能只靠查询条件里的当前时间"、"数据库排他约束使用稳定状态 HELD/BOOKED"、"确认时锁占位行并使用数据库实际当前时间检查截止值"。

但**没有回答一个必然被追问的问题**：如果过期清理 Worker 挂了 30 分钟，一批 `expires_at` 已过但 `status` 仍是 `HELD` 的行会怎样？

- 按当前的表述，它们**仍然占用排他约束**，也就是**把资源锁死了**——用户搜不到、约不上，而数据看起来全"正常"。
- §8.1 里有一句"获取新占位时也可在受控事务中处理同资源已到期占位"，但这句太轻，且没有说清**顺序**：必须在**同一个事务里先 `UPDATE ... SET status='EXPIRED' WHERE resource_id=? AND status='HELD' AND expires_at <= now()`，再 `INSERT` 新占位**——因为 PostgreSQL 的 `EXCLUDE` 约束在语句级检查，同一事务内先前语句的更新对新语句是可见的，所以顺序反了就会自己和自己冲突。

→ 把这一段展开成 4–5 行，明确"**兜底不依赖 Worker 存活**"这个结论。这是"正确性不依赖异步组件"的一个很好的体现，也是你 §8.1 已经写出来的原则（"排他约束阻止双重占用；Redis 锁可帮助削峰，不能作为唯一正确性保证"）的延伸。

### 1.7 入口意图分类被略过——原型的优点没接住

原型有一个独立层：`agents/task_classification/`（`task_classifier` + `agent_router` + `state_manager` + `unrelated_handler` + `classification_processor`，见 commit `2bd2dba`），并且 `StateManager` 用 `StateEnum.CLASSIFY / APPOINTMENT / CONSULT` + `should_classify()` 做**状态门控**，还有 `can_transition_to()` 做合法迁移校验——这是一个真实的状态机雏形。

你的 §5 说"为什么只有两个在线 Agent"讲得好，但**完全没有说入口怎么把消息分给接待/咨询/人工**。§5.1 第 3 步"构建上下文"直接假设已经知道该进哪个 Agent。

→ 补一小段：**分类用"规则 + 小模型 + 状态门控"三层，且分类结果不改变权限**（这是关键：分类错误不能变成越权）。同时明确：原型的 `should_classify()` 门控思想被保留（在预约流程中收到纯咨询，暂停而不重置槽位——§6.2 已经写了这个行为，但要指出它是从原型继承来的）。

### 1.8 SDK 自带 Bash/Write/Edit，必须显式关闭（实践细节，很显功底）

`tool/_builtin/` 里有 `_bash.py` / `_write.py` / `_edit.py` / `_read.py` / `_glob.py` / `_grep.py` / `_powershell.py` / `_skill.py`。这些是给编码 Agent 用的。

§9 说"首版不向 Agent 提供 Shell、任意 SQL、文件写入或通用 HTTP 请求工具"——方向完全正确，但**没有说这些工具在 SDK 里默认就存在**。写进稿子会变成：

> SDK 内置了 Bash / Read / Write / Edit / Glob / Grep 等编码类工具。预约 Agent 的工具集采用白名单注册，**这些内置工具既不注册也通过 `PermissionMode` 显式 DENY**（双保险，因为工具可能被模型以自造名字调用）。

顺便：`permission/_types.py` 的文档明确写了 `BYPASS` 模式下"Safety ASKs from tools are NOT enforced"，并建议无人值守场景用 `DONT_ASK`。这句话可以直接引用来说明"为什么 Worker 用 `DONT_ASK` 而不是 `BYPASS`"。

---

## 2. P1 · 补强后显著加分

### 2.1 把"已验证事实"钉到 commit 与行号（可直接抄）

§2 现在的写法是"由这几处源码可推断"，并且 §17 说"引用链接为公开分支页面，会随项目变化"。但你有本地 clone，可以把举证钉死。**已替你核对，以下每条都成立（甚至比原文更狠）**：

**问题 1：检查与写入不在同一事务，且无区间排他约束**

- `db/repositories/technician_repository.py:154` `add_schedule()` 内 `with self.session_manager.session_scope() as session:`（第 169 行）——**独立的会话/事务**
- 同文件 `:204` `is_technician_available()` 内另一个 `with self.session_manager.session_scope() as session:`（第 216 行）——**另一个独立事务**
- 检查逻辑只是 `start_time < end_time AND end_time > start_time`（第 220–221 行）的**应用层查询**，没有任何数据库级约束兜底
- `db/models.py:17-25` `TechnicianSchedule` 只有 `id / technician_id / start_time / end_time / status / appointment_id`，**没有 `UniqueConstraint`、没有 `CheckConstraint`、没有 `ExcludeConstraint`、没有复合索引**；`start_time`/`end_time` 是**无时区的 `DateTime`**
- 结论强化：这不是"竞争窗口"这么轻——**两个并发的"检查通过→写入"必然同时成功**（因为写入路径完全不校验重叠），是**确定的超卖缺陷**，不是概率性的竞态。

**问题 2：天气工具失败被伪装成成功（比原稿写得更严重）**

`agents/appointment/appointment_processor.py:21` `class WeatherMCPTool(BaseTool)`：

- 第 34–35 行：无 `OPENWEATHER_API_KEY` → `return "北京今天天气晴朗，温度适宜，建议您注意防晒"`（**硬编码**）
- 第 59 行：HTTP 非 200 → 另一段硬编码文案
- 第 61 行：`except Exception` → 再一段硬编码文案（**连异常都被吞掉**）
- 第 57 行：成功路径里文案写死"**北京**当前天气"，但方法参数是 `city`——**参数与返回不一致**
- 第 63–65 行：`_run()` 用 `asyncio.run(...)` 包 `_arun()`，在已有事件循环内调用会直接 `RuntimeError`（源码注释自认"同步版本 - 不推荐使用"）
- 该类全程未使用 MCP 协议（`aiohttp` 直连 REST），且依赖里确实有 `mcp>=1.0.0`——**类名带 MCP 与是否用 MCP 无关**，你的原判断成立

**问题 3：订单事实缺失 + ID 生成方式**

`services/appointment_service.py:30`：`appointment_id = int(time.time() * 1000)`
`services/appointment_service.py:33-42`：写入后只 `return True`，**丢弃了 `add_schedule()` 返回的 `schedule.id`**；`db/models.py` 里**根本没有 Appointment 表**——排班记录本身就是订单记录。

这一条很有价值：它说明原型**没有"订单"这个聚合**，所以你的 §6.1 引入 `appointment` + `operation` + `resource_allocation` 三张表，是**结构性重建而非优化**。这个论断比"补充了事务"有力得多。

→ 把 §17 的来源列表改成"文件名 + commit + 行号"格式，并在 §2 断言后直接附行号。**同时把两个 commit 写进文首**（"本文基于 agentscope@033a361 与 smart-appointment-ai-agent@2bd2dba 核验"），比现在"核验日期如文首、不将分支链接冒充快照"要硬。

### 2.2 容量假设缺三个关键量化

§3.2 的 Little 定律算得**完全正确**（10,000×6÷43,200 = 1.389；×10 = 14；×5s = 70；×1.5 = 21）。但缺三个面试官很可能算到的量：

1. **数据库连接池 × 副本数 vs `max_connections`**。模块化单体 + 多副本 API/Worker，若每副本 `pool_size=20`，10 个副本就是 200 条连接，加上 Worker 可能打爆 PostgreSQL 默认 100。→ 需要给出"连接预算表"（每进程池大小 × 副本数 + 余量），以及是否引入 PgBouncer。这是**单体多副本的经典坑**，写出来很加分。
2. **WAITING_USER 的存量会话数**。§3.2 只算了"活跃处理位置 70"，但 §5.1 第 6 步说要持久化 WAITING_USER 并结束活跃执行——那么**同时在等待用户的会话有多少**？这直接决定 `task` 表的扫描规模、索引设计和 Worker 的到期扫描频率。建议给出假设（如平均等待 90 秒 × 14 msg/s ≈ 1,260 个活跃任务）。
3. **token/秒 而非仅调用次数/秒**。21 次调用/秒 × 平均输入 token → TPM 需求。§13 有成本口径、§3.2 说"需根据真实输入输出 token 校核"，但**没给数量级假设**，导致"模型服务能力是否够"这个结论无法验证。

### 2.3 中文检索：要有选型判据与切换门槛

§10.1 说了"不把 PostgreSQL 默认英文全文检索直接当成中文 BM25"（很好），但缺：

- **候选方案与判据**：`zhparser` / `pg_jieba`（装进 PG，与 pgvector 同库）vs 应用层 jieba 建倒排 vs 迁移 OpenSearch/ES。给出比较维度：运维成本、与 ACL 过滤的协同、分词词典维护（门店专有名词："肩颈""开背"）。
- **pgvector 的切换门槛**：现在说"小语料优先精确搜索，规模上来后才通过基准选择 HNSW"，但**"小"是多少**？建议给出量化（如 ≤5 万 chunk 用精确搜索，延迟可接受；>20 万必须基准测试），否则这句无法执行。
- **专有名词的分词会直接毁掉召回**——这是中文 RAG 最容易被追问的点，值得单独一句。

### 2.4 规则与文档的"一致性校验"要说是编译期还是测试期

§10.1 说"可执行规则如取消窗口以版本化配置保存，文档用于解释，同一发布流程校验两者一致性"。这句话没说清**怎么校验**，而两种做法的工程含义天差地别：

- **编译期（推荐）**：规则以结构化配置为唯一真相源，文档中涉及规则的段落用占位符渲染生成——**结构上不可能不一致**。
- **测试期**：两边手写，靠测试比对——**必然漂移**。

→ 明确采用前者，并说明"文档中不允许手写可执行规则的字面值"。这是一个低成本、高说服力的改进。

### 2.5 时区与夏令时：用户时区 ≠ 门店时区时的裁决

§6.1 说"时间使用带时区的时间点存储，另保留门店 IANA 时区"。§7 第 1 步说"获取会话参考日期与门店时区"。但没回答：

- **用户在外地/国外下单**（"明天下午 3 点"）→ 按用户设备时区还是门店时区解析？**必须明确并写进确认卡**。
- **DST 跳变**：跨夏令时切换日的预约时长、"这周五"落在 DST 切换日。连锁到店服务跨时区是常态，这是很尖锐的追问点。
- 稿子说"首版默认单个服务、单位容量资源、门店本地时区"——把范围收窄是合理的，但要**明确写出"用户时区与门店时区不一致时的算法"**，不能只靠"默认门店时区"含糊过去。

### 2.6 占位时长与库存影响（业务面，容易被追问）

§7 说要"创建单个短期占位"、确认卡要有"占位截止时间"，但**没有给占位数时长与其对库存的影响**。面试官会问："占位 10 分钟，50 店 × 20 技师，高峰期会不会因为大量未确认占位导致搜不到号？"

→ 补：占位时长建议值（如 5–10 分钟）、单用户同时占位上限、单资源同时占位上限（防止刷占位造成**拒绝服务式占库存**）、占位到期前的提醒与自动延长策略。这既是业务合理性，也体现"考虑过滥用"。

### 2.7 安全缺三块

§12.1 的"默认拒绝 + 纵深防御 + 不承诺靠一句系统提示解决"是正确姿态，但缺：

1. **注入路径清单**：用户消息、知识文档内容、工具返回值、历史摘要、**MCP 工具描述**、模型自造的工具名。每一路的处置不同（工具描述要固定来源与版本；模型自造工具名要白名单拒绝）。
2. **PII 与留存**：手机号、位置、支付信息（虽不接支付）。日志中手机号如何哈希/脱敏、数据留存期、用户删除请求（含偏好与行为记录）如何级联。
3. **模型供应商数据边界**：企业客户会问数据是否出境、是否用于训练、审计留痕。§12.2 说"敏感字段脱敏"是同一类问题的一部分。

### 2.8 评测缺三件可执行的东西

§13 是整篇最雄心勃勃的一节（400 场景 + 7 个维度 + 消融），方向都对。缺：

1. **oracle 的编码方式**：说"由测试 oracle 核对最终数据库"，但没定义 oracle 长什么样。建议：场景 = 输入序列 + 期望终态断言（对 `appointment`/`resource_allocation`/`operation` 三表）+ 期望的**约束**（如"不得出现第二个订单""工具调用序列必须包含 confirm 且参数哈希匹配确认卡"）。
2. **故障注入点矩阵**：§13 列了 8 个必测边界（很好），但没说注入手段（`SIGKILL` 时点、`tc netem` 断网、PG 连接中断/主库切换）以及**这些注入点如何覆盖 12 个写入函数**。给一张"写入函数 × 注入时点"的矩阵才有说服力。
3. **CI 门禁**：哪些进 CI、阈值多少。建议分两层：**确定性不变量（零违规）硬门禁**；**LLM 指标（用区间）软门禁 + 人工复核**。稿子已经说了"有限样本的零违规不能证明绝对零风险"（很好），但没把它变成门禁设计。

### 2.9 发布与回滚流程缺失

稿子大量使用版本号（`quote_version`、方案版本、规则版本、知识版本、提示词版本、工具版本、`schema_version`），但**没有讲发布与回滚流程**：

- 提示词/模型/知识/规则四类版本的灰度与回滚策略
- 知识发布"回滚仅切换已验证版本的发布指针"（§10.1 有）——但**提示词与模型的回滚**没有
- 灰度期间新旧版本共存时，**同一任务的版本归属**（一个任务中途换提示词版本算不算状态不一致？）

→ 补一小节。面试官问"你怎么上线一个改动、怎么回滚"时，现在的稿子答不上来。

### 2.10 P0/P1 范围切分不清

§14 的 P0 列了"身份与租户、服务目录、真实可用性查询、占位/确认、事务约束、幂等、任务持久化、工具契约、来源问答、审计、基本观测与测试"。

问题：**P0 到底是不是多副本**？§12.3 要求"多副本通过数据库任务租约及幂等协调"，§11 要求 fencing token 挡住暂停后恢复的旧实例——但 P0 列表里没有"租约与 fencing"，也没有说 P0 允许单副本。

→ 明确切分，例如：
- **P0**：单副本，靠数据库事务与唯一约束保证正确性（不做 lease/fencing）
- **P1**：多副本，引入任务租约 + fencing token + 死锁退避重试

这样 P0 的范围讲得清，也避免"P0 就要做最难的部分"的质疑。

---

## 3. P2 · 打磨

1. **加目录**。17 节 + 38KB，没有 TOC，面试前速览成本高。
2. **加术语表**。`task / proposal / hold / resource_allocation / operation / confirmation` 六个词承担全部核心语义，必须先定义（面试官会问"proposal 和 hold 什么关系"）。
3. **统一标记体系**。稿子反复说"这是设计假设"，但散落各处。建议统一为 `[已验证]`（有源码/文档证据）、`[设计]`（本文决策）、`[待测量]`（需实验确定）三种前缀，关键段落带上。面试时能一眼说清边界，也能防止被误认为"声称已实现"。
4. **补 3 张能画在黑板上的图**：① 主链路时序（已有文字版 §7）；② 任务状态机（已有文字版 §6.2，画出来更好答辩）；③ 并发占位时序（两个请求争抢同一技师 → 一方 `SLOT_CONFLICT`）。现在只有一张静态架构图，面试讲并发和恢复时手上没图。
5. **补一个失败恢复的 trace 样例**（§14 说要展示"一个失败恢复 trace"，但正文没有）。
6. **错误枚举补 6 项**。§9 现有 8 项，缺：`IDEMPOTENCY_MISMATCH`（同键不同参数）、`NOT_FOUND`、`RATE_LIMITED`、`CONFIRMATION_REQUIRED`（需要确认才能继续）、`HUMAN_TAKEOVER_ACTIVE`、`LEASE_LOST`。
7. **数值自洽性检查**：§13 的延迟目标（简单查询 P95 ≤3s、候选流程 P95 ≤8s）与 §5.1 的预算（最多 6 次工具调用 + 2 次咨询委派）需要一起看。6 次工具调用意味着至少 6 次模型往返，8 秒 P95 要求单次往返远快于 1.3 秒。→ 补"每轮模型调用的**期望值**（而非上限）"来支撑延迟目标，否则目标与预算看起来不匹配。
8. **"我的贡献边界"声明**。面试官最容易质疑"这是不是抄开源项目"。§2 有借鉴表，但需要一段明确的：**哪些是我重新设计的、为什么、代价是什么**，以及"改原项目 vs 重写"的取舍理由。结合 §2.1 的发现（原型根本没有订单聚合），"结构性重建"的论断很有力。
9. **超时后的收尾语义**。§5.1 有"20 秒总执行预算"，但没说超时后是返回部分结果还是明确失败。必须明确：**超时必须返回"正在处理"，绝不静默丢弃已执行写入的结果**（这与 §11"不该做的事"的精神一致）。
10. **模型全挂的降级路径**。§11 有"模型超时 → 有界重试 → 备选模型 → 表单/人工"，但"所有模型都不可用"的整体降级（预约入口降为表单）没有讲。
11. **摘要/上下文压缩的触发与策略**。§6.3 说"摘要帮助控制 token"，但没说何时压缩、压缩后如何保证不丢关键确认状态（§10.2 提了一句"工具原始结果过长时保存引用并裁剪上下文，不能把关键错误状态一起裁掉"，很好，但没有落到压缩策略上）。SDK 的 `on_compress_context` hook 正好是落点。

---

## 4. 面试新增必答题（现有 §15 之外）

1. **"SDK 已经有 Agent Service 了，你为什么自己写接入层？"** → 见 §1.1 的边界表。
2. **"用户确认了，但提交时技师已经请假，怎么办？"** → 见 §1.5，答案是提交事务内重校验依赖事实 + `STALE_PROPOSAL`。
3. **"过期的 HELD 占位还会不会占资源？Worker 挂了会怎样？"** → 见 §1.6。
4. **"为什么用 AgentScope 而不是 LangGraph / AutoGen？"** 现在的答案偏弱（"基于任务约束选择"）。补对比维度：状态持久化与 HITL 恢复、权限引擎、事件模型、多租户/渠道/沙箱生态；并**主动交代原型用的是 LangChain**（`requirements.txt` 有 `langchain>=0.3,<1.0`、`langchain-openai`、`faiss-cpu`），说明迁移原因。
5. **"如果让你重做一遍，你会改什么？"**（高频，考察反思能力）
6. **"这个系统最大的未解决问题/风险是什么？"**（高频，考察诚实与自我认知）——诚实答案可以是"占位时长与库存利用率的权衡没有数据支撑"或"中文检索质量未验证"。
7. **"RAG 是必要的吗？能不能直接用 FAQ 表？"** → 需要给出判据（长尾问题比例、知识更新频率）。
8. **"你怎么知道模型选的时段和你服务端理解的一致？"** → 时区/DST 解析归属服务端、保留原文与候选（§6.3 有基础，但没和时区问题连起来）。

---

## 5. 优先级与工作量建议

| 优先级 | 事项 | 影响 |
|---|---|---|
| **P0-1** | 补"复用 SDK vs 自建"边界表（§1.1） | 消除最大质疑 |
| **P0-2** | 补 SDK 能力映射表（§1.2） | 从"读过 README"升到"读过源码" |
| **P0-3** | 修架构图 6 处（§1.3） | 面试官第一眼看的东西 |
| **P0-4** | 实体表补 5 行（§1.4） | 消除表间矛盾 |
| **P0-5** | 提交时重校验依赖事实（§1.5） | 高频追问点 |
| **P0-6** | 过期占位与排他约束的兜底（§1.6） | 高频追问点 |
| **P0-7** | 举证钉到 commit + 行号（§2.1） | 零成本，收益极高 |
| **P1** | §2.2–§2.10 | 显著加分 |
| **P2** | §3 全部 | 打磨 |

**建议的动作顺序**：先做 P0-7（半小时内可完成，改 §2 与 §17），再做 P0-1/P0-2（需要新增约 1.5 页），然后改图（P0-3），最后补 P0-4/5/6 与 P1。

---

## 6. 待澄清（需要你确认后才能动手改）

1. **AgentScope 的版本口径**：稿子说"AgentScope 2.0"，本地 clone 的 `pyproject.toml` 是 `dynamic = ["version"]`，没有硬编码版本号，且 README 已介绍 2.0。建议统一改为"基于 commit 033a361（2026-09-09）核验的能力"，避免版本号争议。
2. **首版是否多副本**：直接影响 P0 范围（见 §2.10）。
3. **渠道范围**：如果确定首版只做 Web，那么"复用 SDK 的 channel"可以推迟，边界表要相应调整。
4. **评测是否真的要落地**：§13 是 400 场景的承诺。如果没有真实运行的计划，建议**明确标记为"计划"并把目标改成"验证方法"**，而不是给出具体数字目标——否则面试时"你跑了吗"这一问会很难受。
