# 设计评审跟进 v3

评审日期：2026-09-17
评审对象（v2 修订版）：
- `智能预约Agent系统设计.md` v2（689 行 / 78,271 bytes，sha256 `d386972b…`）
- `appointment-architecture.json`（11 组件 / 14 连线，sha256 `ebcb4653…`）+ 对应 HTML
- `评审处理说明.md`、`source-verification.json`、`architecture-verification.json`

方法：逐条对照上一轮 28 项清单；用 `git cat-file -p <commit>:<path>` 独立复算 v2 登记的全部源码哈希与符号行号（与 `git show` 逐字节一致）；对 §18 的三张 Mermaid 图补做解析与渲染验证。

---

## 1. 结论

**v2 闭合了上一轮清单的几乎全部项目，并且修正了我 4 处过度断言。** 更值得注意的是质量重心的转移：

修订前，这份稿子最强的是 §8 的并发/幂等/确认三节；修订后，**最强的部分变成了可复现的核验层**——固定 commit、`git show commit:path`、排除工作区改动、逐文件 SHA-256、符号行号、以及"这不是实现报告"的显式声明。

这是面试里最难被挑战的东西：面试官可以不同意你的架构，但很难否认"我能证明我引用的每一行代码确实存在于那个版本"。而且 v2 主动把**不利于自己**的事实写进去了（本地 AgentScope 有大量未提交改动、JWT 文件不在基线、认证只是临时头、Toolkit 默认是空的、Mermaid 未渲染）。主动暴露限制比多写十页功能清单更有说服力。

按上一轮清单逐条核对的结果：**28 项全部落实**（§4.1 边界表、§5.2 SDK 映射、§6.1 实体补齐、§8.2 依赖事实重校验、§8.1 过期兜底、§5 入口分类、§3.3 三项容量、§10.1 中文检索、§6.4 时区/DST、§7 占位时长、§12.1 安全、§13.1–13.3 评测、§12.4 发布回滚、§14 阶段切分、文首目录与术语与标记、§18 三张图、§11.1 恢复 trace、§9 错误枚举、§5.3 超时/降级/压缩、§15 新增答辩题）。

---

## 2. 我上一轮的错误（v2 纠正正确，这里留档）

这几条是我自己必须认的，写进文档之前先写在这里：

| 我上一轮的断言 | 核实后的事实 | 证据 |
|---|---|---|
| "该 commit 已提供 `_jwt.py` / `_jwks.py` / `_auth.py`" | 这三个是**工作区未跟踪文件**，不在 commit 树中；该 commit 的认证是临时 `X-User-ID` 头 | `git ls-tree 033a361:src/agentscope/app/` 无 jwt/jwks/auth；`deps.py:33` `get_current_user_id`，`:36` 自述 "Temporary header-based identity; will be replaced by JWT auth" |
| "SDK 内置 Bash/Write/Edit，默认就存在" | 空 `Toolkit` 的 basic 组是**空的**；编码工具由 Agent Service 从 workspace 组装 | `tool/_toolkit.py:66 class Toolkit`、`:130 tools=tools or []`；`app/_service/_toolkit.py:134 tools = await workspace.list_tools()` |
| "原型用 `can_transition_to()` 做合法迁移校验" | 方法存在但 `set_state()` 未调用它 | `state_manager.py:31 set_state` 只赋值并 print；`:72 can_transition_to` 未被引用 |
| "这是确定性超卖缺陷，并发必然同时成功" | 准确说法是**可构造的重复占用执行顺序** | v2 的措辞更严谨：SQLite 锁竞争、调度与异常都会影响实际结果 |
| "必须在同一事务内先 `UPDATE → EXPIRED` 再 `INSERT`（否则自己和自己冲突）" | 取决于 `EXCLUDE` 约束的检查时机配置 | v2 改为"本文约束选择立即检查、不延迟的配置，不声称所有 EXCLUDE 约束天然只能语句级检查"，并补齐了完整锁协议（客户配额 → 资源日历 → 受影响占位 → 回收 → 插入） |
| "`quote_version` 变化即 `STALE_PROPOSAL`" | 过粗：新价目表上线不应撤销仍有效的旧报价 | v2 引入"有效期内锁价"契约，比我原建议准确 |

**我的方法错误**：我用 `ls` 读了工作区就当成了 commit 内容，而没有用 `git show commit:path`。v2 指出的这一点是这次评审里最有价值的一条纠正——它把"引用开源项目"从叙述变成了可审计的证据链。

---

## 3. 我独立复核了 v2 的核验层：全部通过

用 `git cat-file -p <commit>:<path>`（与 `git show <commit>:<path>` 输出逐字节一致）重新提取并复算：

**符号行号（抽查 12 处，全部命中）**

| 引用 | 命中内容 |
|---|---|
| `app/deps.py:33` / `:36` | `async def get_current_user_id` / `Temporary header-based identity…` |
| `app/_service/_toolkit.py:134` | `tools = await workspace.list_tools()` |
| `tool/_toolkit.py:66` / `:130` | `class Toolkit:` / `tools=tools or [],` |
| `permission/_engine.py:214` / `:491` | `async def _check_explore` / `async def _check_dont_ask` |
| `tool/_base.py:293` | `async def check_read_only` |
| `middleware/_base.py:13` / `:170` | `class MiddlewareBase:` / `async def on_check_permission` |
| `middleware/_budget.py:21` / `:150` | `class ReplyBudgetControlMiddleware` / `async def on_reasoning` |
| `state/_state.py:209` | `class AgentState(BaseModel):` |
| `middleware/_tracing/_trace.py:117` | `class TracingMiddleware` |
| `event/_event.py:139` / `:443` | `class ModelCallEndEvent` / `class RequireUserConfirmEvent` |

**文件哈希（抽查 7 个，全部与 `source-verification.json` 一致，逐字节相同）**

| 文件 | 复算 sha256（前 16 位） |
|---|---|
| `permission/_engine.py` | `9142e0ecf173542a` ✓ |
| `tool/_base.py` | `ec6e27cd2d695da2` ✓ |
| `tool/_toolkit.py` | `749cdf31c42a7e7a` ✓ |
| `middleware/_base.py` | `e459260c4e8a460c` ✓ |
| `db/models.py` | `9c399cc45ec3e0b6` ✓ |
| `appointment_service.py` | `aae5ac8b57e379bf` ✓ |
| `state_manager.py` | `cab9ee3f245881e6` ✓ |

另外确认：`smart-appointment-ai-agent` 工作区确实干净（v2 的说法成立）；`token/_builtin/_ask_user.py`、`formatter/`、`pipeline/`、`tool/_builtin/_{bash,write,edit,read,glob,grep,powershell}.py` 均在基线 commit 中存在（v2 提及的模块名没有虚指）。

**结论：v2 的核验层可复现、可验证，没有核对不上的引用。** 这一层现在可以直接当答辩材料用：面试官随机指定一行，能当场复算。

> 附注（我的环境踩坑，非文档问题）：zsh 里 `git show $C:src/...` 会把 `$C:s` 当参数修饰符展开而报 `bad substitution`，必须写成 `p=src/...; git show "$C:$p"`。v2 记录的方法是 `git show commit:path`，写法本身没问题。

---

## 4. 我补做的一项验证：§18 三张 Mermaid 图

v2 的 `architecture-verification.json` 诚实记录了 `"mermaid_rendered": false`。这三张图现在是答辩时要在白板上画的依据，值得真跑一次。我补做了：

- 方法：从文档按围栏顺序抽取 3 个代码块 → `mermaid.parse()`（真实文法解析）→ `mermaid.render()`（产出 SVG）
- 结果：**3/3 解析通过，3/3 渲染通过**，无未知关键字、无未知事件类型、无参与方别名冲突
- 产物：`verify/mermaid/`（3 个 `.mmd` 抽取副本 + 3 个 `.svg` 渲染产物 + `mermaid-verification.json` 回执，含 SHA-256）

**能证明**：三张图在任何标准渲染器（GitHub / VS Code / mermaid.live）里都能正常显示，不会在现场报语法错误。

**不能证明**：未在真实浏览器中布局（jsdom 没有 SVG 布局引擎，`getBBox` 是桩函数），所以**不能据此声称文字不重叠、连线不交叉**。视觉布局仍需人工或真实浏览器复核一次。

---

## 5. 剩余问题（按价值排序）

### R1 ★★★ 「业务等待」与「SDK 挂起」是两套机制，恢复语义未声明

**现象**：设计里有两个独立的"等待"概念，但没写谁为准、怎么恢复。

- **业务侧**：§5.1 第 6 步"持久化 WAITING_USER 并结束活跃执行；收到事件后恢复"；§6.2 任务状态含 `WAITING_USER` / `WAITING_RESULT`。
- **SDK 侧**：`agent/_agent.py` 里 reply 会**挂起（park）**并等待外部事件续跑——权限 `ASK` 时置 `ToolCallState.ASKING` 并 yield `RequireUserConfirmEvent`（`:2576-2586`）；外部工具时置 `SUBMITTED` 并 yield `RequireExternalExecutionEvent`（`:2613-2626`）。续跑方式是**把事件喂回同一个 `reply()`**：`UserConfirmResultEvent | UserInterruptEvent | ExternalExecutionResultEvent`（`:1040-1049`），挂起状态存在 `AgentState` 里（`state/_state.py:328 has_awaiting_tool_calls`）。

**为什么面试官会问**："用户 10 分钟后回了一句'好'，你是**续跑那个挂起的 reply**，还是**重建上下文开一个新 reply**？"

**建议补一段明确规则**，二选一并写清代价：

- **（推荐）重建式**：业务账本为准，SDK 挂起态视为**可丢弃的瞬时态**。恢复时按 §6.3"先读结构化状态和订单，再构建模型上下文"重新进入。代价：放弃 `ReplyBudgetControlMiddleware` 跨中断的计数连续性（或改为在 task 账本里自己累计 token），换来"崩了也能恢复、不依赖 SDK 内部状态"。
- **续跑式**：必须把完整 `AgentState` 与 reply_id 持久化，并接受两个后果——① `agent/_agent.py:1054-1059` 明确写了：用 HITL 事件续跑时**你新传入的 `structured_schema` 会被忽略，沿用挂起 reply 的 schema**，这会影响 §12.4"任务固定 release 版本"的规则（旧 release 的输出契约会被沿用）；② 人工接管/任务 epoch 变更后，必须显式拒绝把旧事件喂给挂起的 reply，否则 SDK 会继续在旧循环里发工具调用（好在 §4/§8.2 的工具边界会按 epoch 拒绝，但要说清是"依赖工具边界兜底"而不是"SDK 自己会停"）。

**无论选哪个**，都应该补一句：**业务 `WAITING_USER` 与 SDK 挂起态不一致时，以业务账本为准。**

### R2 ★★★ `AskUser` 是最容易被误用为"确认"的入口

**现象**：§5.2 只提了 `RequireUserConfirmEvent`（"普通 SDK 工具许可不等于同意某个预约方案"——这个判断正确）。但基线 commit 里还有一个更危险的东西：**内置工具 `AskUser`**。

- `tool/_builtin/_ask_user.py:195` → `is_external_tool: bool = True`（对比 `tool/_base.py:113` 默认 `False`）→ 它**会让 reply 挂起**，与 R1 直接耦合：如果用 `AskUser` 做追问，那么每一次追问都会挂起 reply。
- `_ask_user.py:187 metadata_schema` + `:182` 注释："The caller also returns the same answers in `ToolResultBlock.metadata`, shaped by `AskUserMetadata`, **for programs that must branch on the choice rather than read prose**"；`:127-128` 更直白："a step deciding whether its work was **approved** cannot do that on prose"。

也就是说，`AskUser` 的返回是**专门设计成可以被程序当作"批准"来分支**的。这正是 §8.3 要防的失效模式——"LLM 输出的 confirmation=true 不具有授权效力"——的**工具化版本**：不是模型编的，是 SDK 提供的、看起来像结构化批准的通道。

**建议**：在 §5.2 或 §8.3 明确一句：

> `AskUser` / `RequireUserConfirmEvent` 的答复属于**用户输入事件**，只可作为槽位补丁或意图信号；它们**不构成 §8.3 的方案级确认凭据**。业务确认只能由绑定 `proposal_id/version/内容哈希` 的确认协议产生，且必须经领域服务在提交事务内重校验。

顺带把 `is_external_tool=True` 与 §5.3"要保存完整 AgentState"的关系写清：**用 `AskUser` 追问 = 挂起 reply = 必须能持久化并恢复 AgentState**；如果不打算承担这个，就应该让追问走应用自己的 SSE 交互，而不是让 Agent 调 `AskUser`。

### R3 ★★ `outbox / job` 实体缺供应商侧字段

图示 `notice`（通知供应商）的 sublabel 是"发送 / 回执 / 对账"，§11 与 §13.2 都要求"按供应商能力对账"、"外部不明结果按供应商能力对账"。但 §6.1 的 `outbox / job` 字段是"event_id、aggregate_id/version、类型、投递状态、重试次数、下次执行时间"——**没有任何供应商侧字段**（供应商消息 ID、回执状态、对账状态/批次、最后查询时间）。

→ 补 3–5 个字段即可（如 `provider`、`provider_message_id`、`receipt_status`、`reconciled_at`）。这是实体表与图/正文的最后一处不一致，且恰好落在你最强调"残留风险要明说"的那条链路上。

### R4 ★★ 门店/坐席侧的写命令没有契约

§3.1 的不变量明确包含"用户、**门店员工**和后台任务都经过同一预约领域服务，不能绕过约束写排班"；§4 也说"业务 API 同样调用这些领域服务"；§6.1 这轮还新增了 `business_calendar / closure`（停工日/临时闭店）与 `handoff_case`。但 §9（标题是**"工具契约与服务 API"**）只有 9 个**用户侧**工具，全是给 Agent 用的。

于是这些问题没有落点：**谁写临时闭店/请假？谁把订单置 `COMPLETED`/`NO_SHOW`？坐席改派技师走哪个命令？** 另外 §11 说坐席能看到"相关订单和工具结果摘要"，但 §12.1 的 PII 最小化只约束了**模型输入**，没有约束**坐席可见范围**，也没说坐席的写操作是否同样走确认凭据与审计。

→ 建议在 §9 加一张 3–4 行的小表（门店/坐席侧命令：闭店、请假、改派、核销、置 NO_SHOW），并补一句坐席权限范围与审计要求。这是**你自己声明的不变量与接口面之间的缺口**，面试官顺着 §3.1 往下读很容易问到这里。

### R5 ★ §13.3 的示例预算与 §13 的 P95 目标是同一个数

§13 表："标准候选流程 P95 ≤8 秒"；§13.3："示例预算为模型累计 5 秒、并行查询和重排 1 秒、排队/网络/应用开销 2 秒" → 5+1+2 = **8 秒**。

如果这个预算是**期望值**，那么把目标也定成 8 秒等于**零余量**：模型调用与数据库延迟都是右偏分布，任何尾部延迟都会把 P95 推过 8 秒。文中的"期望 2–3 次模型往返"也在暗示 8 秒是典型路径而非上限。只有把它理解为**上限**才自洽。

（说明：这里不能说"P95 必然大于均值"——对右偏分布通常成立，但数学上并非恒真，所以只主张"零余量"这个更弱、更稳的结论。）

→ 一句话澄清即可：说明这 8 秒是**单次请求的硬预算上限**（超时按 §5.3 分类返回），而"P95 ≤8 秒"是**实测目标**、需另行验证；或者把示例预算下调（如 4+1+1=6 秒）给 P95 留出余量。这是全篇唯一一处我能算出来的数值内部矛盾。

---

## 6. 建议动作

| 优先级 | 动作 | 说明 |
|---|---|---|
| **1** | 补 R1（两套等待/恢复语义） | 唯一一个**架构级**未决问题，且决定 §5.3/§12.4 的写法 |
| **2** | 补 R2（AskUser 不是确认凭据） | 与 R1 同源，一起改；一句话就能堵住一个高频追问 |
| **3** | 补 R3 字段（3–5 个） | 消除实体表与图/正文的不一致 |
| **4** | 补 R4 门店/坐席命令表 | 消除不变量与接口面的缺口 |
| **5** | 澄清 R5 数值口径 | 一句话 |
| **6** | §18 图做一次人工视觉复核 | 我已验证解析/渲染通过，但布局未验证 |
| 可选 | 把 `verify/mermaid/` 的产物登记进 `architecture-verification.json` | 让 `mermaid_rendered: false` 有对应回执 |

R1–R5 都不需要重写结构，属于局部增补；改完这份稿子在"能不能被追问倒"这个维度上基本没有明显缺口了。
