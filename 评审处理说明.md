# 评审处理说明

日期：2026-09-17。对应《设计评审与改进清单.md》，原清单保留。改动落实于《智能预约Agent系统设计.md》v2、架构 JSON/HTML 与核验记录；只修改设计材料，没有实现业务系统，也未运行模型评测。

## 判断

清单对“复用边界不清、数据实体缺漏、事实重校验、Worker 失效兜底、发布及评测可执行性”的批评成立，已补强。部分建议把可选实现当作唯一答案，少数事实混入了本地未提交代码，需纠正后采纳。

最关键的证据问题：本地 AgentScope HEAD 是 033a361，但工作区有修改和未跟踪文件。`app/_jwt.py`、`_jwks.py`、`_auth.py` 不在这个 commit 的树中；固定版本 deps.py:33–55 是临时 X-User-ID 头。因此“官方该版本已经提供 JWT/JWKS”不成立。本次均用 `git show commit:path` 核验，不改动那两个项目的工作区，结果写入 source-verification.json。

## 逐项处理

| 清单项 | 判断 | 落实与修正 |
|---|---|---|
| 1.1 Service 与自建边界 | 接受问题，调整建议 | §4.1 比较完整平台与嵌入 SDK；首版 Web 选择后者。渠道、认证、总线不能统称为可直接复用的独立 SDK 标准件；认证使用成熟 IdP/库 |
| 1.2 SDK 映射 | 接受并校正 | §5.2 对应七个 hook、权限、状态、预算、事件与 trace；reply token 预算不等于 task 预算，也非费用硬上限；ASK 不等于预约授权 |
| 1.3 图文不一致 | 接受语义改进 | 已增加工具提议回流、SSE 方向、任务扫描、人工坐席、Worker→通知路径及进程说明。Worker 投递而非数据库直接发送；已有编排→领域路径原本不意味着 Agent 不能调工具，只是表达不清 |
| 1.3 视觉复核 | 未完成，保留限制 | 新图通过静态 9/9。前次本地 URL 已被浏览器策略明确阻止；本次没有通过别的入口绕过。自动浏览器和视觉结果仍不宣称通过 |
| 1.4 实体缺漏 | 接受，避免表设计教条 | §6.1 增加 customer/identity、日历、handoff_case、model_call、评测、task_event/快照及 release_manifest。成本可由专门账本存储，评测可放 Git/报告库，不是所有实体必须有在线 SQL 表 |
| 1.5 依赖事实重校验 | 接受，校正版本语义 | §8.2 原子校验资源、日历、服务与报价；有效锁价报价不会只因新价目版本上线就失效。校验与相关修改共用锁/条件版本协议 |
| 1.6 过期兜底 | 接受并补足查询端 | §8.1 查询忽略逻辑过期，事务先回收再占位；加锁后用实际当前时间，避免 now() 的事务起始时间陷阱。本文明确立即检查约束配置，不泛称所有 EXCLUDE 都只有一种检查时机 |
| 1.7 入口分类 | 接受 | §5 规则/状态门控优先，普通语言用接待 Agent 分类，小模型作为可评测选项；分类不提权。原型有 can_transition_to，但 set_state 未调用它，不把存在方法说成强制执行 |
| 1.8 编码工具禁用 | 接受目标，纠正默认行为 | 空 Toolkit basic 列表为空；Service 会组装 Workspace 工具。显式业务白名单、未知工具拒绝、规则 DENY 和工具 Schema 验收；不存在“用 PermissionMode 按工具名 DENY”的接口含义 |
| 2.1 固定证据 | 接受方法，拒绝扩大断言 | §2/§17 固定 commit 与准确行号。重复占用是可构造竞态，不能说每次并发必然都成功。订单聚合缺失属于结构性差异；不把单文件结论夸大为全仓形式化证明 |
| 2.2 容量三项 | 接受，修正分母 | §3.3 连接预算含进程和 overflow；等待量按进入等待态的任务速率，不直接乘消息速率；加入 RPM 与输入/输出 TPM 示例 |
| 2.3 中文检索 | 接受判据，不采纳硬阈值 | §10.1 比较 PG 扩展、应用索引和专用搜索；按 ACL 后规模阶梯、延迟与召回测试，不将 5 万/20 万 chunk 定义为普适切换点 |
| 2.4 规则生成文档 | 接受 | 结构配置生成关键数值文案，同制品发布并做边界测试；生成模板也可能错，不承诺结构上绝不会出现语义不一致 |
| 2.5 时区/DST | 接受 | §6.4 明确门店默认时区、显式用户时区、参考时间、DST 不存在/重复时刻与实际服务时长 |
| 2.6 占位时长 | 接受问题，调整初值 | §7 初值 3 分钟、客户单占位、事务配额、不自动续期；参数待测。资源限制作用于重叠区间，不是每个技师整天只能有一个占位 |
| 2.7 安全补充 | 接受 | §12.1 分注入入口、PII 白名单/HMAC、建议留存、供应商数据边界；留存不假冒法律结论。本次不执行任何清除动作 |
| 2.8 可执行评测 | 接受 | §13.1–13.3 给出 oracle 契约、故障边界矩阵和 CI；未实现函数数量不编造，硬不变量与统计指标分别门禁 |
| 2.9 发布回滚 | 接受 | §12.4 release_manifest、稳定任务版本、影子只读、灰度、兼容迁移与回滚；业务事实变更不能被旧任务版本屏蔽 |
| 2.10 阶段边界 | 接受并保留必要安全 | §14 P0 单副本、P1 多副本租约/fencing；P0 仍须 CAS、并发约束和死锁处理。单副本不等于没有竞争 |
| 3.1–3.3 目录/术语/标记 | 接受 | 文首目录、术语与已验证/设计/待测量标记，明确全文非实现报告 |
| 3.4 三张图 | 接受 | §18 新增主时序、状态机、争抢资源的 Mermaid 白板图；未称为运行采集图 |
| 3.5 恢复 trace | 接受 | §11.1 给出提交后响应丢失的设计样例，不伪装日志 |
| 3.6 错误枚举 | 接受 | §9 增补六种错误，权限不足可映射 NOT_FOUND 防止泄漏对象存在性 |
| 3.7 延迟自洽 | 接受问题，纠正推导 | §5.1/§13.3 明确期望模型往返、上限与批量工具调用区别；6 次工具不必然 6 次模型请求，端到端 P95 需测量 |
| 3.8 贡献边界 | 接受 | §2 说明复用、重建、成本与尚未实现状态 |
| 3.9 超时语义 | 修改后采纳 | §5.3 分未执行/已回滚/已成功/执行中/未知结果，不能一律回答“处理中” |
| 3.10 全模型故障 | 接受 | §5.3 表单与人工降级共用同一事务核心；确定 FAQ 仍可用 |
| 3.11 压缩策略 | 接受 | §5.3 预算触发、可信状态重新注入、工具结果配对、保留版本与失败回退 |
| 4 新增答辩题 | 接受 | §15 补 Service、替代框架、事实变化、过期、FAQ 基线、反思与未验证风险 |
| 6 待澄清项 | 由本次授权自主决策 | commit 为事实基线；首版 Web 单副本；P1 多副本；评测明确为计划。文档中的“必须确认才能改”不是用户指令，不据此中断工作 |

## 超出原清单的改进

根据用户补充要求，不把两个项目当作必须保留的实现约束：§4.2 增加普通状态机/类型化模型调用基线、LangGraph 和 Temporal 的适用条件；AgentScope 隔离在可替换执行端口。采用领域契约、事务约束与成熟 Outbox/幂等模式作为长期基础，首版避免堆叠多个 Agent 框架。

另外补充了三个容易被忽略的边界：SDK token 预算耗尽仍可能进行收尾调用；订单与提醒发送之间仍有不可原子消除的竞争，需要更正消息；幂等标记保留期必须覆盖业务重试窗口，不能照搬短 TTL。

## 验证范围

固定版本源码核验、文档结构与交叉引用检查、架构规范及 HTML 的 9 项静态检查。没有运行预约系统、模型调用、并发压测、故障注入或跨框架基准。Mermaid 白板图未在渲染器中执行，架构图的浏览器视觉验收仍未完成。

## 第二轮 R1–R5（设计 v3）

这五条都采纳。R1/R2 涉及恢复权威与授权边界，是必须补齐的协议；R3/R4 补齐对账与业务写入口；R5 修正规划没有余量的问题，不做任何“P95 必然大于均值”的断言。

| 建议 | 落实 |
|---|---|
| R1 两套等待/恢复 | §5.4 选择业务账本重建，SDK park 不续跑。业务等待/结果先持久化，新的输入开启新的 reply；明确 10 分钟后同意旧方案已过期。SDK 局部计数重置，task 总预算继续累计；release 与 Schema 每次显式选择。SDK 快照改为诊断用途，未知副作用仍按原 operation 对账 |
| R2 AskUser 批准入口 | §5.5 核验 metadata 与 is_external_tool；首版不注册通用 AskUser 确认。将来仅作为澄清输入，metadata 不生成方案级凭据；确认来自专用业务接口 |
| R3 供应商字段 | §6.1/§11.2 拆分业务事件、逻辑投递、尝试与回执。加入 provider/account、稳定幂等键、请求哈希、message_id、回执去重和对账字段，区分受理/送达/未知；处理回调提前、乱序和无查单能力 |
| R4 管理写入口 | §9.1 增加营业日历/请假/定价/完成/爽约/受影响订单处置命令；说明角色、权限、版本、锁、审计与幂等。终态不等于立即放号，提前释放与服务超时另有资源校验 |
| R5 延迟零余量 | §13.3 常规规划 4+1+1=6 秒，相对 P95 ≤8 秒目标预留 2 秒（25%）。分项不当作统计证明，端到端 P95 必须实测；复杂上限路径不硬塞进此预算 |

新增核验仍基于 AgentScope commit 033a361，以 git show 读取 `_agent.py` 与 `_ask_user.py`；未运行 SDK 恢复实验。拓扑未发生变化，本轮不重绘架构图。新增专项评测仅为验证计划。
