# 人工测试指引

面向"用真实进程把整条链路跑一遍"的人工验证。所有命令都在仓库根目录执行。

## 0. 前置：数据库与依赖

```bash
docker ps --filter name=appointment-pg        # 5433 端口，必须 Up
ls .venv/bin/python                           # 虚拟环境已就绪
```

数据库必须是 PostgreSQL：核心不变量"不超卖"依赖 `btree_gist` 区间排他约束，
应用启动时会校验这条约束，缺少它宁可启动失败。

## 1. 装载种子数据（拿身份头）

```bash
.venv/bin/python scripts/dev_reset.py
```

会清空全部业务表并灌入演示夹具，然后打印可直接复制的身份头：

- `X-Tenant-Id` / `X-Store-Scope`：租户与门店
- `X-Actor-Id` / `X-Customer-Id` / `X-Role`：客户（林女士、赵先生各一套）
- 店长（`X-Role: store_manager`）与员工（`X-Role: staff`）身份
- `service[shoulder]` / `service[back]` 等服务 ID

参考时刻取**真实当前时间**，所以"明天下午"就是真的明天下午。

## 2. 起服务

```bash
.venv/bin/python -m uvicorn appointment.api.app:create_app --factory --port 8000
curl -s http://127.0.0.1:8000/healthz      # 期望 status=ok
```

> 本机若设了 HTTP 代理，访问 127.0.0.1 时加 `--noproxy '*'`（curl）或
> `trust_env=False`（httpx）。

## 3. 主链路：一条消息走到预约成功

设好身份头（下面用 `H` 代指这一串 `-H`），然后按顺序发：

```bash
H='-H "Content-Type: application/json" -H "X-Tenant-Id: <T>" -H "X-Actor-Id: <A>" \
   -H "X-Role: customer" -H "X-Customer-Id: <C>" -H "X-Store-Scope: <S>"'

# 3.1 提出需求 → 期望 PROPOSED，回复里带候选时段
curl -s -X POST localhost:8000/v1/messages $H \
  -d '{"client_message_id":"m1","text":"明天下午想做肩颈按摩","store_id":"<S>"}'

# 3.2 选定时段 → 期望 WAITING_CONFIRMATION，pending_confirmation 里带
#     proposal_id / proposal_version / confirmation_token / expected_task_version
curl -s -X POST localhost:8000/v1/messages $H \
  -d '{"client_message_id":"m2","text":"第一个","store_id":"<S>"}'

# 3.3 确认（六个字段都必填）→ 期望 committed_status=CONFIRMED，replayed=false
curl -s -X POST localhost:8000/v1/confirmations $H \
  -d '{"proposal_id":"<P>","proposal_version":<V>,"confirmation_token":"<CT>",
       "client_confirmation_event_id":"cc-1","idempotency_key":"idem-1",
       "expected_task_version":<TV>}'

# 3.4 原样再确认一次 → 期望同一个 operation_id，且 replayed=true（幂等重放）
```

**要盯的点**：3.4 必须返回 `replayed: true` 和与 3.3 相同的 `appointment_id`
`operation_id`——同一个凭据只产生一次预约效果。

## 4. 事件流

```bash
# 从头重放
curl -s localhost:8000/v1/tasks/<TASK_ID>/events/replay?from_sequence=0 $H

# SSE：从游标 11 续订，结束后应收到 caught_up
curl -N localhost:8000/v1/tasks/<TASK_ID>/events $H -H "Last-Event-ID: 11"
```

保留窗口外的游标会返回 `RESET_REQUIRED`（要求客户端全量重放，而不是静默丢事件）。

## 5. 异步投递：Worker 与回执

预约成功只写 Outbox，真正的通知投递由独立 Worker 完成：

```bash
# 单轮跑一遍（也可以 python -m appointment.worker 常驻）
.venv/bin/python -c "
import asyncio, logging; logging.basicConfig(level=logging.INFO)
from appointment.worker.__main__ import serve
asyncio.run(serve(owner='manual', max_iterations=3))"
```

期望日志里 `outbox_processed=1`、`deliveries_sent=1`，并且账本分层齐备：

```bash
docker exec appointment-pg psql -U appointment -d appointment -c \
  "select status, channel, template_version from notification_delivery;"
docker exec appointment-pg psql -U appointment -d appointment -c \
  "select attempt_number, status, provider_message_id from delivery_attempt;"
```

`ACCEPTED` 只代表供应商受理。要走到 `DELIVERED`，需要供应商回调：

```bash
# 用上面查到的 provider_message_id
.venv/bin/python scripts/dev_receipt.py --message-id sbx-xxxx --status delivered \
  --base-url http://127.0.0.1:8000

# 故意超出重放窗口：应被隔离，不改状态
.venv/bin/python scripts/dev_receipt.py --message-id sbx-xxxx --status delivered \
  --issued-at-offset -900 --base-url http://127.0.0.1:8000
```

**要盯的点**：

- 回调入口是全系统唯一不带租户身份的路由，身份来自 `(provider, account_id)`
  路径 + 签名 + 时间窗口；`payload` 自报的 tenant 一律忽略。
- 认不出/验不过的回执**照样入库隔离**，不是丢弃——一批过期签名往往就是重放尝试。
- 未知事件类型只记录、不改状态；晚到的 `accepted` 不能把 `DELIVERED` 降级。

## 6. 边界与越权（应当失败）

```bash
# 请求体自带 tenant_id → 400 VALIDATION_ERROR（身份不能由请求体声明）
curl -s -X POST localhost:8000/v1/messages $H \
  -d '{"client_message_id":"x","text":"hi","tenant_id":"<T>"}'

# 换个租户的身份头读别人的任务 → NOT_FOUND（不是 403：不泄露存在性）
# 用过期/篡改的签名回调 → action=REJECTED，且回执被隔离
```

## 7. 浏览器侧（本仓库自带的 `frontend/`）

上面 1–6 节测的是**机器客户端**接口（`/v1/*`，curl 级别）。要让浏览器里有人能点，
走这一节：`webapp/` 把 AgentScope 的 Web UI 接到了同一套领域层上，前端就在本仓库的
`frontend/`（无需外部 agentscope checkout）。

### 起三个进程

```bash
# ① 业务库种子（顺带清掉本演示的 AgentScope 会话键）
.venv/bin/python scripts/dev_reset.py

# ② 浏览器侧服务 —— 端口 8010，不是 3000（本机 3000 被 Grafana 占着）
DEEPSEEK_API_KEY=sk-xxx .venv/bin/python -m webapp.service

# ③ 前端（本仓库自带；首次要先 pnpm install）
cd frontend && pnpm install && pnpm dev
```

②的启动横幅会列出本机可登录的演示身份，例如：

```
  可登录的演示身份：
  customer-1（林女士）  角色=customer  客户 8fe91b79-...
  customer-2（赵先生）  角色=customer  客户 63ea6568-...
  manager（店长）  角色=store_manager  租户 5ce69bc8-...
```

也可以 `curl http://127.0.0.1:8010/demo/identities` 取（仅开发期身份模式下开放）。

### 在浏览器里验收

打开 http://localhost:5173 ，在 setup 页填：

- **服务器地址**：`http://127.0.0.1:8010`
- **用户名**：`customer-1`

进去后应当看到左侧已有一个智能体「门店预约助理」和一个会话「门店预约（只读）」。
问一句 `肩颈多少钱？明天下午有空档吗？` ，期望：

- 出现一张可展开的工具卡片，写着 **调用 2 次工具**，展开后是
  `get_service_quote` 与 `search_availability`——**没有** Bash / Read / 团队等框架工具；
- 回答里有真实报价（`268 元` / `60 分钟`）和一张可用时段表（时间 / 技师 / 房间）；
- 回答明确写着"**只读快照，未占位**"，并说明下单这一步要去门店或客服渠道。

左侧「智能预约」是独立的业务页面，不把写工具暴露给聊天模型。用顾客登录后可继续验收：

- 输入预约需求，页面从 `/booking/v1/messages` 推进任务，并从持久事件 SSE 更新事件游标；
- 出现方案后，页面显示服务、时段、时长和价格，明确要求用户再点击「确认预约」；
- 确认请求断线时，页面保留原幂等键。点「查询最新状态」会先查该键对应的操作，再刷新
  任务快照；若仍是待确认，则重新取得短期凭据，要求用户再次显式确认；
- 刷新页面后检查确认卡恢复。浏览器存储只保留任务 ID 和幂等标识，不保存确认凭据；
- 店长、未知用户名和无身份请求都不能取得预约上下文。

**要盯的点**：

- 模型可见的工具参数里**没有** tenant / store / customer 任何身份字段（展开工具卡片
  看参数，或看 `tools.py` 里那三个函数的签名）。身份是装配工具时由服务端闭包捕获的。
- 刷新页面后历史应当恢复（Redis 落库 + SSE 重放）。
- 用 `manager` 登录应当看到**空白的智能体列表**——店长没有 `customer_id`，给不出
  `ToolScope`，解析器明确拒绝而不是给一个空作用域。

### 浏览器控制台里的非致命噪音

会看到几条红色错误，都是 order_demo 的功能面板，本服务**有意不提供**：

- `GET /knowledge_bases/` → 503（未配知识库面板；知识检索走的是 `search_knowledge`
  业务工具，不依赖它）
- `GET /handoff/...` → 404（转人工）
- `GET /refund-proposals/session/...` → 404（退款提案）

聊天本身不受影响。

若不想手点，可以跑仓库里的端到端冒烟（用系统 Chrome，不下载 Chromium）：

```bash
node scripts/webui_smoke.mjs        # ②③ 都起着时跑，截图落在 docs/screenshots/
```

它断言的三件事与上面手工验收清单一致：工具卡片只含业务工具、回答里有真实报价与
可用时段、并写明"只读 / 未占位"边界。

## 8. 已知边界（不要当成缺陷）

- **聊天模型工具只读**：只装配了 `get_service_quote` / `search_availability` /
  `search_knowledge`，模型无法调用预约写入。顾客可通过独立「智能预约」页面查看方案并
  显式确认；写入仍由 `/booking/v1/*` 服务端路由调用原领域事务完成。
- **浏览器侧的认证是开发期模式**：`identity_provider=None` 时用 `X-User-ID` 请求头
  当登录名。这**不是**生产认证，正式环境要换成 `BearerPrincipalProvider`。
- **机器客户端接口仍然只有 HTTP API**：`src/appointment/api/static/` 不存在，根路径
  不挂载 UI。浏览器界面来自 `webapp/` + 仓库自带的 `frontend/`（AgentScope web_ui 的
  工作树副本，含多租户支持所需的本地改动），见第 7 节。
- **没有 Alembic 迁移**：库表由 `create_all` 建立（开发与测试够用），
  还没有生产环境的迁移路径。
- **机器客户端默认不发起真实 LLM 调用**：默认 `APPOINTMENT_MODEL_BACKEND=stub`，
  `APPOINTMENT_AGENT_RUNTIME=deterministic`，所以回复是确定性文本。（浏览器侧相反：
  它用 `DEEPSEEK_API_KEY` 走真实模型。）
- **知识检索只有关键词一路**：向量路留待启用 pgvector。没有字面重叠的转述
  式提问（"可以退定金吗"）会返回"未检索到相关片段"，这是**诚实拒答**而不是
  编答案——但也确实是召回率的已知短板。
